# RecipeMate API Environment Variables Template
# Copy this file to .env and fill in actual values

# =====================================================
# Database Configuration
# =====================================================
DB_USERNAME=recipemate
DB_PASSWORD=your_secure_password_here

# =====================================================
# Redis Configuration
# =====================================================
REDIS_PASSWORD=your_redis_password_here

# =====================================================
# Food Safety Korea API Key
# =====================================================
# Get your API key from: https://www.foodsafetykorea.go.kr/api/
# 1. Visit the website and sign up (free for development)
# 2. Go to "공공데이터활용 신청" -> "오픈API 신청"
# 3. Copy your API key and paste below
FOOD_SAFETY_API_KEY=your_api_key_here

# =====================================================
# Cloudinary Configuration (Image Upload Service)
# =====================================================
# Get your credentials from: https://cloudinary.com/console
# Sign up at https://cloudinary.com (free tier available)
# Go to Dashboard and copy your credentials
CLOUDINARY_URL=cloudinary://<api_key>:<api_secret>@<cloud_name>

# =====================================================
# Kakao Maps API Configuration
# =====================================================
# Get your JavaScript key from: https://developers.kakao.com/console
# 1. Create an application at Kakao Developers Console
# 2. Go to "앱 설정" -> "플랫폼" and add your domain (e.g., http://localhost:8080)
# 3. Copy the "JavaScript 키" and paste below
KAKAO_JAVASCRIPT_KEY=your_kakao_javascript_key_here

# =====================================================
# Application Configuration
# =====================================================
SPRING_PROFILES_ACTIVE=prod

# =====================================================
# Optional: JVM Configuration
# =====================================================
# Uncomment and modify if you need custom JVM settings
# JAVA_OPTS=-XX:+UseContainerSupport -XX:MaxRAMPercentage=75.0 -XX:+UseG1GC

# =====================================================
# Setup Instructions:
# =====================================================
# 1. Copy this file:
#    cp .env.example .env (Linux/Mac)
#    copy .env.example .env (Windows)
#
# 2. Edit .env and replace all placeholder values:
#    - DB_PASSWORD: Use strong password (16+ chars, mixed case, numbers, symbols)
#    - REDIS_PASSWORD: Use strong password (16+ chars)
#    - FOOD_SAFETY_API_KEY: Get from foodsafetykorea.go.kr
#
# 3. For development, you can use:
#    DB_PASSWORD=recipemate2024!dev
#    REDIS_PASSWORD=redis2024!dev
#    FOOD_SAFETY_API_KEY=sample (limited functionality)
#
# 4. For production, use secrets manager:
#    - AWS Secrets Manager
#    - Azure Key Vault
#    - HashiCorp Vault
#    - Kubernetes Secrets
#
# 5. IMPORTANT:
#    - Never commit .env file to Git (.gitignore excludes it)
#    - Change default passwords immediately
#    - Rotate passwords regularly (every 90 days recommended)
#    - Use different passwords for each environment (dev/staging/prod)
