# RecipeMate API - Development Environment
# 로컬 개발 환경 설정

# =====================================================
# Spring Profile
# =====================================================
SPRING_PROFILES_ACTIVE=dev

# =====================================================
# Database Configuration (Docker PostgreSQL)
# =====================================================
DB_USERNAME=recipemate
DB_PASSWORD=recipemate2024!dev

# =====================================================
# Redis Configuration
# =====================================================
REDIS_PASSWORD=redis2024!dev

# =====================================================
# Food Safety Korea API Key
# =====================================================
# 개발용 샘플 키 또는 실제 키 입력
# Get your API key: https://www.foodsafetykorea.go.kr/api/
FOOD_SAFETY_API_KEY=86b90dadcc914700a12b


# =====================================================
# Recipe 초기화 및 동기화 설정
# =====================================================
# 초기 데이터 로드 활성화 (Docker 최초 실행 시)
RECIPE_INIT_ENABLED=true

# 강제 초기화 (DB에 데이터 있어도 재로드)
RECIPE_INIT_FORCE=false

# 스케줄링 비활성화 (로컬 개발 시 불필요)
RECIPE_SYNC_ENABLED=false

# =====================================================
# 사용법 (Docker Compose)
# =====================================================
# docker-compose --env-file .env.dev up -d
#
# 또는 docker-compose.yml에서 env_file 지정:
# services:
#   app:
#     env_file: .env.dev
